<h1><%= @contact.first_name %> <%= @contact.last_name %></h1>

Email:<p><%= @contact.email %></p>

Phone Number:<p><%= @contact.phone_number %></p>

<a class="btn btn-outline-primary" href="/contacts/<%= @contact.id %>/edit">EDIT</a>

<%= form_tag "/contacts/#{@contact.id}", method: :delete do %>
  <input type="submit" class="btn btn-outline-danger" value="DELETE CONTACT">
<% end %>

<a class="btn btn-outline-primary" href="/contacts">BACK TO ALL CONTACTS</a>